<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cassette">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_app_light_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_app_light_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.MusicPlayer">
        <activity
            android:name="com.example.cassette.views.SplashScreen"
            android:theme="@style/Theme.noActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.cassette.views.MainActivity"
            android:theme="@style/Theme.MusicPlayer"></activity>
<!--        <activity android:name=".views.PlayMusicActivity" />-->
    </application>

</manifest>

    <!--TODO("add these two lines to main activity")
    <category android:name="android.intent.category.APP_MUSIC" />
    <category android:name="android.intent.category.DEFAULT" />
    -->